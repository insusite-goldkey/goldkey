# 골드키 약관 RAG 업로드 API 환경변수 예시
# 이 파일을 복사해서 .env 로 저장하고 실제 값을 채워주세요.
# cp env.example .env

# ── 인증 ──────────────────────────────────────────────────────
# Chrome Extension에서 보낼 X-API-Key 헤더 값 (임의의 긴 문자열 권장)
API_KEY=your-secret-api-key-here

# ── GCS ───────────────────────────────────────────────────────
# GCS 버킷명
GCS_BUCKET=goldkey-policy-rag

# 서비스 계정 키 JSON 경로 (Cloud Run/GCE에서는 ADC 사용 → 이 항목 불필요)
GCS_KEY_PATH=/path/to/service-account-key.json

# ── CORS ──────────────────────────────────────────────────────
# Chrome Extension ID가 확정되면 아래처럼 제한 (쉼표 구분)
# ALLOWED_ORIGINS=chrome-extension://abcdefghijklmnopabcdefghijklmnop

# ── 파일 크기 제한 (MB) ────────────────────────────────────────
MAX_FILE_MB=50

# ── RAG 인제스트 (선택 — rag_ingest.py 뼈대 활성화 시) ────────
# SUPABASE_URL=https://xxxx.supabase.co
# SUPABASE_KEY=your-supabase-key
