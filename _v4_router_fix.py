# -*- coding: utf-8 -*-
"""gk_risk 라우터를 war_room 앞으로 이동 — 최우선 배치"""

src = open('D:/CascadeProjects/app.py', encoding='utf-8-sig').read()

OLD = (
    "    # ═══════════════════════════════════════════════════════════════════════\n"
    "    # [GP102/103] war_room 라우터 — 실전 상담 전략실\n"
    "    # ═══════════════════════════════════════════════════════════════════════\n"
    "    if cur == \"war_room\":\n"
    "        show_war_room()\n"
    "        st.stop()\n"
    "\n"
    "    # ═══════════════════════════════════════════════════════════════════════\n"
    "    # [GK-RISK] 전략적 위험 분석 사령부 라우터\n"
    "    # ═══════════════════════════════════════════════════════════════════════\n"
    "    if cur == \"gk_risk\":\n"
    "        _render_gk_risk()\n"
    "        st.stop()\n"
)

NEW = (
    "    # ═══════════════════════════════════════════════════════════════════════\n"
    "    # [GK-RISK] 전략적 위험 분석 사령부 라우터 — ★ 최우선 배치\n"
    "    # ═══════════════════════════════════════════════════════════════════════\n"
    "    if cur == \"gk_risk\":\n"
    "        _render_gk_risk()\n"
    "        st.stop()\n"
    "\n"
    "    # ═══════════════════════════════════════════════════════════════════════\n"
    "    # [GP102/103] war_room 라우터 — 실전 상담 전략실\n"
    "    # ═══════════════════════════════════════════════════════════════════════\n"
    "    if cur == \"war_room\":\n"
    "        show_war_room()\n"
    "        st.stop()\n"
)

cnt = src.count(OLD)
print(f"OLD anchor count: {cnt}")
assert cnt == 1, "앵커를 찾을 수 없습니다"

src = src.replace(OLD, NEW, 1)
open('D:/CascadeProjects/app.py', 'w', encoding='utf-8').write(src)
print("✓ gk_risk 라우터 최우선 배치 완료")
print(f"총 줄 수: {len(src.split(chr(10)))}")
