# 골드키 CRM — React Native 앱

보험 영업 담당자를 위한 지능형 CRM 모바일 앱입니다.

## 📁 프로젝트 구조

```
crm_app/
├── App.js                          # 진입점
├── package.json                    # 패키지 의존성
├── ios_info_plist_guide.md         # iOS 17 달력 권한 설정 가이드
└── src/
    ├── store/
    │   └── crmStore.js             # Zustand 전역 상태 스토어
    ├── screens/
    │   └── Dashboard.js            # 메인 대시보드 화면
    └── components/
        ├── TaskCard.js             # 할 일 카드 (memo + Selector 최적화)
        ├── SilsonBadge.js          # 실손 세대 배지
        └── CalendarSync.js         # 기기 달력 동기화 (iOS 17 대응)
```

---

## ⚡ 5대 핵심 기능

| # | 기능 | 구현 위치 |
|---|------|-----------|
| 1 | **Tab UI (IA)** — 기본/보험 정보 점진적 공개 | `Dashboard.js` Sub-Tab |
| 2 | **실손 세대 자동 산출** — 1~4세대 배지 | `crmStore.js` `calculateSilson()` |
| 3 | **iOS 17 달력 동기화** — 권한 예외 처리 | `CalendarSync.js` |
| 4 | **Zustand CRM 대시보드** — Selector 최적화 | `crmStore.js` + `TaskCard.js` |
| 5 | **게이미피케이션** — Haptic + Confetti | `TaskCard.js` + `Dashboard.js` |

---

## 🚀 설치 및 실행

### 1. 의존성 설치

```bash
cd crm_app
npm install

# iOS Pod 설치
cd ios && pod install && cd ..
```

### 2. Metro 서버 실행

```bash
npm start
```

### 3. 앱 실행

```bash
# Android
npm run android

# iOS
npm run ios
```

---

## 📦 핵심 패키지

| 패키지 | 버전 | 용도 |
|--------|------|------|
| `zustand` | ^4.5.2 | 전역 상태 관리 |
| `react-native-calendar-events` | ^2.2.0 | 기기 달력 동기화 |
| `react-native-haptic-feedback` | ^2.2.0 | 촉각 피드백 |
| `react-native-confetti-cannon` | ^1.5.2 | 100% 달성 축하 애니메이션 |

---

## 🍎 iOS 17 App Store 심사 통과 필수 설정

`ios/GoldkeyCRM/Info.plist`에 아래 내용을 추가하세요. **없으면 리젝됩니다.**

```xml
<key>NSCalendarsWriteOnlyAccessUsageDescription</key>
<string>고객 보험 만기일 및 갱신일 알림을 달력에 등록하기 위해 접근 권한이 필요합니다.</string>

<key>NSCalendarsUsageDescription</key>
<string>고객 보험 일정 확인 및 만기일 관리를 위해 달력 접근 권한이 필요합니다.</string>
```

상세 가이드: `ios_info_plist_guide.md` 참조

---

## 🧠 아키텍처 설계 원칙

### Zustand Selector 패턴 (리렌더링 최적화)

```js
// ❌ 비효율 — tasks 변경 시 모든 구독 컴포넌트 리렌더
const store = useCrmStore();

// ✅ 최적화 — 해당 task 하나만 구독, 다른 task 변경 시 리렌더 안 됨
const task = useCrmStore(useCallback((s) => s.tasks.find((t) => t.id === taskId), [taskId]));
```

### 실손 세대 판별 기준

| 세대 | 가입 기간 | 주요 특징 |
|------|----------|-----------|
| 1세대 구실손 | ~2009.09 | 면책 없음, 보장 가장 넓음 |
| 2세대 표준화실손 | 2009.10~2017.03 | 항목별 보장 한도 |
| 3세대 착한실손 | 2017.04~2021.06 | 비급여 특약 분리 |
| 4세대 실손 | 2021.07~ | 비급여 실적 연동 보험료 |
