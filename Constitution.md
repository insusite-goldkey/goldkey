# 🏛️ 골드키 통합 가이딩 프로토콜 (GoldKey Integrated Constitution)

> 이 문서는 골드키 앱의 모든 UI/UX, 기술 구현, 브랜드 경험에 관한 최상위 설계 원칙입니다.  
> AI 및 개발자는 아래 가이딩 프로토콜 조항을 항상 준수해야 합니다.

---

## 제6조 [프리미엄 그라데이션 박스]
골드-오렌지 계열 그라데이션을 활용하여 주요 액션 블록을 시각적으로 강조한다.

---

## 제7조 [레드 얼럿 시스템]
경고·오류 상황에는 빨간색 계열 배경과 흰색 텍스트를 사용하여 즉각적인 주의를 유도한다.

---

## 제8조 [공간 분할]
화면을 논리적 섹션으로 분리하여 사용자의 시선 흐름을 자연스럽게 유도한다.

---

## 제9조 [컴팩트 레이아웃]
불필요한 여백을 제거하고, 한 화면 내 최대한의 정보를 효율적으로 배치한다.

---

## 제10조 [스플래시 화면 시스템]
앱 초기 로딩 시 전체화면 스플래시 오버레이를 표시하며, 로딩 완료 시 페이드아웃 처리한다.  
최소 노출 시간은 0.3초이며, DB 초기화 완료 또는 최대 1.0초 경과 시 강제 해제한다.

---

## 제20조 [상단 공간 최적화 (Top Space Optimization)]
콘텐츠가 화면 최상단에 즉시 노출되도록 Streamlit 기본 헤더, 툴바, 장식 요소 등 모든 상단 여백을 제거한다.

---

## 제26조 [전문가 통합 지식 인덱스]
도메인별 문서를 체계적으로 분류하고, 최종 업데이트 일시 및 핵심 근거를 함께 관리한다.

---

## 제28조 [블록 식별 번호 (BID) 시스템]
개발 모드에서 모든 UI 블록에 고유 식별자(BID)를 표시하여 디버깅 효율을 높인다.

---

## 제29조 [다중 인증 로그인 흐름]
Phase A(본인 확인) → Phase B(OTP + 보안수단 선택) → Phase SETUP(등록) → Phase C(실제 로그인) 순서를 준수한다.

---

## 제30조 [반응형 기기 최적화 (Device Adaptation)]

모든 UI(특히 스플래시 화면)는 '반응형 레이아웃'을 적용한다.  
접속 기기의 가로 너비를 실시간 감지하여, 핸드폰(Portrait)과 태블릿(Landscape) 모드에 맞는 최적의 이미지 비율을 자동 선택한다.  
스플래시 로고와 애니메이션은 화면 중앙 정렬을 원칙으로 하며, 화면 크기에 따라 로고 크기를 30%~50% 범위 내에서 유동적으로 조절한다.  
기기 종류에 상관없이 텍스트 가독성을 확보하기 위해, 최소 폰트 크기는 14px 이상을 유지한다.

---

## 제31조 [기기별 스플래시 이미지 분리 적용 (Dual Splash Strategy)]

모든 접속 기기에서 최적의 브랜드 경험을 제공하기 위해, 핸드폰과 태블릿용 스플래시 이미지를 별도로 운영한다.

### §1 모바일 최적화
가로 너비 **768px 미만** 환경에서는 세로형 전용 이미지(`splash_mobile.png`)를 호출하여 화면의 상하 여백을 최소화한다.  
실제 파일명: `Image_for_phone_splash_04c6295711.jpeg`

### §2 태블릿/PC 최적화
가로 너비 **768px 이상** 환경에서는 가로형 전용 이미지(`splash_tablet.png`)를 호출하여 광활한 가로 해상도를 100% 활용한다.  
실제 파일명: `Tablet_splash_screen_7b65c0fcd7.jpeg`

### §3 전환 부드러움
기기 회전 또는 크기 변경 시 이미지가 즉각 교체되되, 시각적 충격을 줄이기 위해 **0.5초의 페이드(Fade) 효과**를 강제한다.  
배경 레이어(`#gk-sp-bg`)의 `opacity`를 0 → 1로 전환하여 이미지 교체 충격을 최소화한다.

### §4 구현 우선순위
1. `window.innerWidth` 기준 실시간 감지 (DPR 보정 포함)
2. 768px 경계에서 이미지 분기 (`< 768` → mobile, `≥ 768` → tablet)
3. `resize` 이벤트 감지 → 경계 통과 시에만 이미지 교체 실행
4. 교체 시 `opacity: 0 → 1` fade 0.5s ease-in-out 적용

---

## 제32조 [CFP 기반 라이프사이클 및 의학경제학적 보장 분석 표준]

> AI는 고객의 경제적 가치를 산정할 때 반드시 이 조항의 공식과 논리를 최우선으로 적용한다.

### 제1항 [의학경제학적 가처분 소득 역산 원칙]

고객의 경제적 가치를 객관적으로 산출하기 위해 **당해 연도 전체 건강보험료율(2024년 기준 7.09%)**을 적용한 역산법을 최우선 지표로 삼는다.

**추정 월 소득 산출 공식:**
```
추정 월 소득 = 납입 건강보험료 ÷ 0.0709
```

**일일 경제적 가치(필요 일당) 산출 공식:**
```
일일 필요 일당 = 추정 월 소득 ÷ 30일
```

예시: 건보료 25만 원 납입 시 → 월 소득 약 352만 원 → 일일 가치 약 12만 원

### 제2항 [상해후유장해 소득 대체 로직 — 2년 원칙]

상해로 인한 10~15%의 영구장해 발생 시, 신체적 손실을 넘어 **'시간의 상실'**을 보전하는 것을 목적으로 한다.

- **소득 대체 기간:** '치료 및 적응기(1년)' + '재취업/직장적응기(1년)' = 총 **2년**의 소득 공백 전제
- **적정 가입 금액:** 10% 장해 시 2년치 소득을 온전히 보전할 수 있도록 역산 소득 기반 설계, **최소 3억~5억 원 필수 방어선** 권고

### 제3항 [의학적 근거에 따른 질병 및 간병 설계]

질병의 판정 기간과 현대 의학의 치료비 현실을 반영하여 실질적 비용 공백을 메운다.

| 질환 | 기준 기간 | 설계 기준 |
|------|----------|----------|
| **중풍(뇌졸중)** | 국가 영구장해 판정·요양 지원까지 최소 **18개월** | 18개월치 이상 소득 대체 + 간병 비용 |
| **치매** | 확진 후 장기요양 신청 가능 경과 **6개월** | 최소 6~8개월치 간병 비용 |
| **암 진단비** | 조기암(5천~1억)과 별개 | 진행성/전이암: 표적·면역항암제 비용 반영, **최소 1억~3억 원** 권장 |

### 제4항 [CFP 기준 라이프사이클 연금 설계]

은퇴 후 삶의 질을 결정하는 소득 대체율을 CFP(국제공인재무설계사) 표준에 따라 구성한다.

**3층 연금 구조:**
- 국민연금(40~45%) + 퇴직연금(10%)의 한계를 인정
- **개인연금이 현 소득의 40~50%를 보전**할 수 있도록 설계

### 제5항 [지능형 담보 매칭 및 보전 전략 — Gap Filling]

금융감독원(FSS)의 합산 한도 제한으로 인해 발생하는 보장 공백을 전략적으로 해결한다.

- **한도 초과 대응:** 계산된 일일 필요 일당이 금감원 한도(상해 7만 / 질병 10만 등)를 초과할 경우 차액을 즉시 산출
- **담보 복합 설계:** 차액을 보전하기 위해 상해/질병 수술비, 1~5종 수술비, N대 질병 수술비 등을 조합하여 입원 시 실제 수령액이 고객의 일일 경제적 가치에 수렴하도록 최적화

### 제6항 [전문가 브리핑 및 리포트 작성 표준]

분석 결과 제시 시 고객의 신뢰를 확보하기 위해 다음의 용어와 논리를 강제한다.

- **핵심 용어 사용:** 단순 금액 설명 대신 **'의학경제학적 휴업손해 보전'** 용어 사용
- **논리적 연결:** "왜 수술비 담보가 필요한가?"에 대해 건강보험료 역산 결과(고객의 실질 가치)와 연동하여 설명 — 설계사 개인의 주관이 아닌 **데이터 기반 객관적 진단**임을 명시
- **브리핑 표준 문구:**
  > "고객님의 건강보험료 기반 일일 경제적 가치는 OO만 원이나, 규정상 일당은 OO만 원까지만 가입 가능하므로 부족분 OO만 원을 수술비 담보로 대체 보완하였습니다."

---

## 제34조 [시스템 자율 유지보수 및 법정 요율 최신화 (Autonomous Maintenance)]

> 이 조항은 앱이 시간이 지나도 '낡은 지식'을 전달하지 않도록 방어하는 **'디지털 방부제'** 역할을 한다.

### 제1항 [법정 요율 정기 모니터링]

시스템은 매년 1회(매년 1월 1일~5일 사이) 보건복지부 및 국민건강보험공단의 공시 자료를 탐색한다.

모니터링 대상:
- 당해 연도 전체 **건강보험료율** (2024년 기준 7.09%)
- **장기요양보험료율**
- **산재보험 요율** 등 역산 알고리즘에 사용되는 모든 법정 수치

### 제2항 [알고리즘 자율 보정]

- 요율 변동이 확인될 경우, 제32조에 명시된 역산 공식의 **분모(Denominator) 수치**를 즉각 자동 수정한다.
- 수정된 수치는 즉시 **컨설팅 슬라이더 및 시뮬레이터**에 반영된다.
- 코드 내 `0.0709` 하드코딩 상수는 **`_CURRENT_NHIS_RATE`** 동적 변수로 관리하며, Supabase `system_config` 테이블에 저장·로드한다.

### 제3항 [업데이트 로그 및 투명성 확보]

- 요율이 변경될 때마다 관리자(설계사)에게 **인앱 알림 배너**를 발송한다.
- 가이딩 프로토콜 문서 하단 `[Update Log]` 섹션에 자동 기록한다.
- 고객 리포트 하단에 다음 문구를 자동 갱신한다:
  > "본 분석은 {연도}년 {월}월 공시된 최신 요율(건강보험료율 {요율}%)을 기준으로 작성되었습니다."

### 제4항 [지식의 동적 확장]

- 요율 외에도 **금감원 지침 변경** 및 **의학적 신기술**(신규 고가 항암제 등) 출시 정보를 정기적으로 학습한다.
- 제32조의 보장 기준(암 진단비 등)을 상향 제안할 수 있도록 시스템을 최적화한다.
- 가이딩 프로토콜 제28조(동적 지식 확장 시스템)와 연계하여 시스템 신뢰성을 유지한다.

### §구현 핵심 상수 (app.py)

| 상수/변수명 | 역할 |
|------------|------|
| `_CURRENT_NHIS_RATE` | 현재 적용 중인 건강보험료율 (동적, Supabase 로드) |
| `_CURRENT_LTCI_RATE` | 현재 장기요양보험료율 (동적) |
| `_ART34_RATE_UPDATED_AT` | 최종 요율 갱신 일시 |
| `_ART34_RATE_SOURCE` | 요율 출처 URL/문서명 |
| `_art34_rate_check()` | 요율 점검 + Supabase 업데이트 + 알림 생성 함수 |

---

## 제35조 [치매 및 퇴행성 질환 전문 보장 및 자율 업데이트 원칙]

> AI는 치매(알츠하이머)와 파킨슨병이라는 특수 리스크를 다룰 때 이 조항을 최상위 가이드라인으로 적용한다.

### 제1항 [의학적 골든타임 준수 원칙 — Leqembi/CDR 0.5~1]

알츠하이머 신약(레켐비·도나네맙 등)의 임상 효능은 **초기 단계(CDR 0.5~1)**에 집중된다.
따라서 중증 치매보다 **'경도/중등도 치매' 시점**에 고액의 진단비와 치료비가 집중되도록 설계 로직을 우선 가동한다.

- **비급여 검사 보전:** 아밀로이드 PET-CT 및 뇌척수액(CSF) 검사 등 확진 검사비(약 200만 원)를 `비급여 검사비` 또는 `치매 정밀 검사비` 담보로 최우선 배정한다.

### 제2항 [파킨슨병(G20) 즉시 대응 및 산정특례 로직]

파킨슨병은 진단 즉시 **중증 질환 산정특례 등록**이 가능하여 본인 부담금이 10%로 낮아진다(뇌졸중과의 핵심 차이).

- 파킨슨병 컨설팅 시 `수술비`보다 `진단비` 및 `산정특례 대상 진단비` 담보를 최우선 순위로 설정한다.
- 가처분 소득의 300% 이상 효율을 달성하도록 설계한다.

### 제3항 [특정 담보 명칭 기반 정밀 매칭]

실제 치료 프로세스에 맞춘 담보 명칭 기반 정밀 매칭을 수행한다.

| 분류 | 권장 담보명 | 비고 |
|------|------------|------|
| **치료비** | 알츠하이머병 특정치료비 | 레켐비 등 연간 투약비 타겟 |
| **돌봄비** | 장기요양급여 지원금(재가/시설), 간병인 사용 일당(체증형) | 장기 돌봄 대비 |
| **진단비** | 경도이상 치매 진단비(CDR 1), 특정 퇴행성 질환 진단비(파킨슨 등) | 골든타임 집중 |

### 제4항 [국가 정책 및 의학 지표 자율 업데이트]

시스템은 매년 1회(1월) 다음 사항을 자동 검색하여 분석 기준에 반영한다.

- **국가 제도:** 장기요양보험 등급별 급여액 변화, 산정특례 대상 질병 확대 여부
- **의학 기술:** FDA/KFDA 승인 신규 치매 치료제(도나네맙/키선라 등) 출시 여부 및 임상 데이터
- **로직 자동 보정:** 신약 출시·제도 변경 시 담보 추천 리스트와 필요 가입 금액 기준을 스스로 수정하고 설계사에게 보고한다.

### 제5항 [의학경제학적 휴업손해 보전 시뮬레이션]

- **공식 연동:** 제32조(건보료 역산) 일당 기준(예: 12만 원)을 적용한다.
- **효율 비교:** "수술비 1,000만 원" 단독보다 "레켐비 치료비 2,000만 원 + 산정특례 진단비 1,000만 원" 조합이 가처분 소득 방어에 압도적임을 시각적 리포트로 증명한다.

### §구현 핵심 상수 (app.py)

| 상수/변수명 | 역할 |
|------------|------|
| `_ART35_LEQEMBI_ANNUAL_COST` | 레켐비 연간 치료비 기준액 (원) |
| `_ART35_PET_CT_COST` | 아밀로이드 PET-CT 검사비 기준액 (원) |
| `_ART35_PARK_COPAY_RATE` | 파킨슨 산정특례 본인부담률 (0.10) |
| `_ART35_CDR_STAGES` | CDR 단계별 권장 담보 딕셔너리 |
| `_ART35_COVERAGE_MAP` | 질환별 우선 담보 명칭 매핑 딕셔너리 |
| `_art35_calc_report()` | 담보 효율 비교 리포트 생성 함수 |
| `_ART35_LAST_SYNC` | 마지막 자율 업데이트 일시 |

---

## 제36조 [유니버설 시각 디자인 시스템 — Universal Visual System]

> 앱의 시각적 요소는 고객의 신체적 조건에 맞춰 유동적으로 변화하며 최상의 가독성을 제공한다.

### 제1항 [돋보기(시니어) 모드 통합 스케일링 — 1.2x]

- 버튼 활성화 시 텍스트·아이콘·이미지 요소를 일괄 **1.2배(120%)** 확대한다.
- 레이아웃 보존을 위해 모든 블록의 여백(Padding)·간격(Gap)도 비례 조정한다.
- `body.gk-senior` 클래스 토글 방식으로 전역 적용한다.

### 제2항 [고대비 모드 강제 적용 — High Contrast]

- 돋보기 모드 활성 시 텍스트 색상을 순수 검정(`#000000`) 또는 진한 가독성 컬러로 강제 전환한다.
- 배경-텍스트 대비율 **7:1 이상** 유지 (WCAG AAA 기준).

### 제3항 [실시간 상태 알림 — Visual Feedback]

- 모드 활성화 즉시 **"시니어 맞춤 모드가 활성화되었습니다"** 안내 배너를 **3초간** 노출한다.
- 활성 중 우상단에 돋보기 배지(`🔍`)를 상시 표시한다.

### 제4항 [핵심 지표 전략적 강조 — 1.5x Super Scaling]

- **[암 진단비], [가처분 소득], [일일 가치], [치매 생활비]** 핵심 수치는 모드 무관하게 **기본 대비 1.5배** 고정 확대한다.
- `.gk-kpi` 클래스로 지정하며 Extra Bold + 강조색을 적용한다.

### 제5항 [치명적 담보의 레드 하이라이트 — Red Alert Focus]

- 돋보기 모드 활성 시 **[사망보험금(종신/정기)]** 및 **[중증 장해 급여]** 항목에 `.red-alert-focus` 클래스를 적용한다.
- 스타일: 글자색 `#FF0000`, 폰트 두께 900, 심장 박동형 펄스(Pulse) 애니메이션으로 시선 강제 고정.

### §구현 핵심 상수 및 클래스 (app.py)

| 항목 | 역할 |
|------|------|
| `body.gk-senior` | 돋보기 모드 전역 클래스 |
| `.gk-kpi` | 핵심 지표 1.5x 슈퍼 스케일링 클래스 |
| `.red-alert-focus` | 사망보험금·중증장해 레드 펄스 클래스 |
| `--ui-scale` | CSS 변수 (기본 1.0 → 시니어 1.2) |
| `gk_senior_mode` | `st.session_state` 토글 키 |

---

## 제37조 [유연한 인증 및 시스템 안정성 원칙 (Smart Auth Fallback)]

> 시스템은 **'가문 안보'**의 중요성에 따라 보안과 상담 연속성의 균형을 최우선으로 한다.

### 제1항 [보안의 실용성 — OTP 폴백(Fallback)]

- 생체인식 시스템의 하드웨어적 불안정성이 감지될 경우, 시스템은 즉시 **6자리 인증번호(OTP/SMS) 기반의 2단계 인증**으로 전환한다.
- 안정화 전까지 **'아이디/연락처 + 6자리 인증번호'** 체계를 표준 프로토콜로 채택한다.
- 생체인증(`지문/Face ID`) 옵션은 라이브러리 충돌 해결 후 **선택 옵션**으로 재활성화한다.

### 제2항 [단계적 고도화 — 생체인증 잠정 비활성화]

- Phase SETUP의 **지문인식 선택 버튼**은 안정화 완료 전까지 비활성화(disabled) 상태로 표시한다.
- 비활성화 상태에서 해당 옵션은 **"준비 중"** 안내 문구와 함께 회색으로 렌더링한다.
- 백그라운드에서 생체인증 API 충돌 여부를 지속 점검하고, 해결 시 즉시 재활성화한다.

### 제3항 [UX 최적화 — OTP 입력창 대형화]

- OTP 6자리 입력창은 **가이딩 프로토콜 제36조(돋보기 모드)** 원칙에 따라 대형 폰트로 배치한다.
- 입력 글자 크기: **최소 2rem 이상**, 중앙 정렬, 굵은 자간(letter-spacing: 0.3em 이상).
- 설계사가 현장에서 번호를 빠르고 정확하게 입력할 수 있도록 입력 오류를 최소화한다.

### 제4항 [세션 연장 — 상담 연속성 보장]

- 로그인 성공 후 상담 세션은 **2시간(7,200초)** 동안 유지된다.
- 비활동 세션은 2시간 경과 시 자동 만료된다.
- 인증 대기 화면 상단에 **"🛡️ 가문 안보를 위한 트리플 보안 가동 중"** 문구를 필수 표시한다.

### §구현 핵심 상수 (app.py)

| 상수/변수명 | 역할 |
|------------|------|
| `SESSION_TTL` | 세션 유지 시간 (초, 기본 7200 = 2시간) |
| `_session_checkin()` | 세션 체크인 및 만료 처리 함수 |
| `_get_session_remaining()` | 잔여 세션 시간(초) 반환 |
| `hlp_bio_tog` (disabled) | 지문인식 버튼 — 안정화 전 비활성화 상태 |

---

## 제38조 [전략적 경량화 및 지능형 배치 처리 원칙 (Strategic Lightweighting & Intelligent Batch Protocol)]

> 앱의 **민첩성**을 극대화하고, 지식의 깊이는 **야간 학습**을 통해 채운다.  
> 앱은 낮에는 설계사의 상담을 돕고, 밤에는 스스로 공부하여 다음 날 더 나은 답변을 준비한다.

### 제1항 [시각적 부하 제거 — UI Lightweighting]

- 페이지 전환 및 블록 이동 시 발생하는 **모든 돋보기 애니메이션, 암전 효과, 복잡한 렌더링 프로세스(CSS filters, JS heavy effects)를 소스 코드에서 완전히 삭제**한다.
- 모든 화면 전환은 **페이드인(Fade-in) 또는 즉시 표시(Instant Display)** 로 대체하여 체감 속도를 제로화한다.
- 화면 전체를 어둡게 만드는 다크 오버레이(`gk-loading-overlay`)와 스피너는 폐기하며, `gk-show` 클래스 부여를 중단한다 (하위 호환 DOM 유지).
- GCS 데이터 로드 대기 중에는 **해당 블록의 형태를 미리 보여주는 스켈레톤 UI를 최우선으로 렌더링**한다.
- 체감 대기 시간 목표: **0.5초 이내**.
- 표준 구현 클래스: `.gk-skeleton` (shimmer loader), `transition: opacity 0.25s ease-in-out`.

### 제2항 [핵심 단어 추출 및 저장 — Daytime Keyword Logging]

- 상담 중 발생하는 질문에 대해 **전체 텍스트 분석 대신, 명사 위주의 핵심 키워드(Key Words)만을 추출**하여 가벼운 JSON 형태로 별도의 **야간 학습 대기열(Pending Queue)** 에 기록한다.
  - 예: `"레켐비 부작용"`, `"산정특례 갱신"` 등 핵심 단어 단위 저장.
- **낮 시간대에는 분석을 멈추고 기록만 수행**한다 — 실시간 무거운 처리는 야간으로 위임한다.
- Pending Queue 저장 항목: `핵심 키워드`, `질문 원문`, `발생 시각(ISO 8601)`, `컨텍스트(탭/기능명)`, `해결 여부`.
- 저장소: `st.session_state["_pending_queue"]` (세션 내 누적) → Supabase `pending_wisdom` 테이블 비동기 저장.
- 기록 트리거 조건: LLM 응답에 `검색 필요`, `확인 후 안내`, `정보 없음`, `추가 확인`, `정확한 정보가 없`, `확인이 필요` 패턴이 포함된 경우.

### 제3항 [야간 자율 지능형 캐싱 — Night-time Intelligence]

- 앱 사용량이 최저인 **심야 시간대(02:00~04:00 KST)** 에 시스템은 스스로 저장된 키워드를 분석하여 심층 자료를 검색한다.
- 야간 자율 프로세스 순서:
  1. Supabase `pending_wisdom` 로드 → `resolved=False` 항목 최대 20건 추출
  2. Google Search 및 LLM(Gemini)을 통해 정교한 답변과 시각 자료 생성
  3. 결과물을 **'상담 준비 패키지'** 형태로 GCS `consulting_cache/{md5}.json` 저장 → 다음 날 즉시 호출 가능
  4. GCS `wisdom_cache.json` 갱신 (새 인사이트 목록 누적)
  5. Supabase `pending_wisdom.resolved = True` 플래그 갱신
- 야간 프로세스 실행 코드에는 `# [제38조 §3] 심야 전용 — 02:00~04:00 KST` 주석을 필수 표기한다.
- **현재 구현 수준**: `_art38_night_worker()` 완성 (Supabase 연동 + Gemini+GoogleSearch 호출 + GCS 저장).

### 제4항 [상담 브리핑 준비 — Morning Insight Banner]

- 야간 학습이 완료된 경우, 설계사님이 앱을 구동할 때 **'밤새 정리된 새로운 인사이트'** 알림 배너를 최상단에 표시한다.
- 배너 표시 조건: `wisdom_cache.json`에 새 항목이 존재하며 `shown: false`인 경우.
- 캐시 메모리 전진 배치: 새 인사이트를 `st.session_state["_wisdom_ready"]`에 로드하여 해당 질문이 다시 들어올 때 즉시 응답 (**Cache-First 전략**: 로컬 세션 캐시 → GCS 순).
- 배너 확인 후 `shown: true` 플래그 갱신 → 중복 표시 방지.

### 제5항 [성능 개선 보고 의무 — Performance Report]

- 제38조 조치 적용 후, **앱의 초기 로딩 속도 및 페이지 전환 속도 개선 수치를 수치로 보고**할 의무가 있다.
- 보고 기준: 조치 전/후 체감 대기 시간(ms) 및 GCS 호출 횟수 비교.
- 캐시 히트율(consulting_cache hit rate)을 지속적으로 모니터링하여 야간 학습 효율을 평가한다.

### §구현 핵심 상수 및 함수 (app.py)

| 항목 | 역할 |
|------|------|
| `.gk-skeleton` | Pre-fetch 대기 shimmer 로더 클래스 |
| `#gk-loading-overlay` | 구형 다크 오버레이 — `gk-show` 부여 중단 (하위호환 유지) |
| `_ART38_NIGHT_START` | 야간 자율 프로세스 시작 시각 (기본값: `"02:00"`) |
| `_ART38_NIGHT_END` | 야간 자율 프로세스 종료 시각 (기본값: `"04:00"`) |
| `_ART38_PENDING_KEY` | Pending Queue 세션 키 (`"_pending_queue"`) |
| `_ART38_WISDOM_KEY` | Morning Insight 세션 키 (`"_wisdom_ready"`) |
| `_ART38_PENDING_TRIGGERS` | 미결 감지 키워드 tuple |
| `_art38_log_pending()` | 핵심 키워드 추출 + Pending Queue 기록 + Supabase 비동기 저장 |
| `_art38_is_pending_response()` | LLM 응답 미결 키워드 감지 → 로깅 트리거 판단 |
| `_art38_cache_get()` | consulting_cache Cache-First 조회 (세션 → GCS) |
| `_art38_cache_put()` | consulting_cache GCS 비동기 저장 |
| `_art38_load_wisdom()` | GCS `wisdom_cache.json` 로드 → 세션 전진 배치 |
| `_art38_mark_wisdom_shown()` | 배너 확인 후 `shown=True` 갱신 (GCS 비동기) |
| `_art38_insight_banner_html()` | 새 인사이트 알림 배너 HTML 반환 |
| `_art38_skeleton_html()` | GCS 로드 대기 중 shimmer 스켈레톤 HTML 반환 |
| `_art38_night_worker()` | 심야 배치 워커 (Supabase → Gemini+Search → GCS) |

---

## 제39조 [즉각적 접근성 및 초기 로딩 최적화 원칙 (Instant Accessibility & Initial Load Optimization Protocol)]

> 앱 구동 시 불필요한 단계를 제거하고, 설계사님이 즉시 상담에 임할 수 있도록 속도를 보장한다.

### 제1항 [정적 스플래시 이미지의 네이티브화 — Native Static Landing]

- 앱 구동 시 로직이 실행되기 전, **기기 자체에서 띄우는 네이티브 초기 화면(Launch Screen)으로만** 스플래시 이미지를 사용한다.
- 앱 내부의 별도 스플래시 컴포넌트(`st.image` 스플래시, 인트로 애니메이션 등)와 관련 CSS 애니메이션은 **전면 삭제**한다.
- 스플래시 단계에서의 연산은 0이어야 한다 — 이미지 렌더링만 허용.

### 제2항 [사이드바 및 메인 화면 우선 렌더링 — Main-First Rendering]

- 앱 실행 즉시 **가장 가벼운 메인 대시보드와 사이드바의 뼈대를 최우선으로 화면에 노출**한다.
- 사이드바 내부의 무거운 데이터(프로필 이미지, 실시간 통계, GCS 데이터 등)는 화면이 뜬 직후 **비동기(Asynchronous)로 채워 넣는다**.
- 비동기 로딩 대기 중에는 제38조 §1의 `.gk-skeleton` shimmer 로더를 해당 블록에 적용한다.
- **렌더링 우선순위**:
  1. 사이드바 뼈대 + 메인 탭 레이아웃 (즉시)
  2. 사용자 세션 캐시 로드 (즉시)
  3. 프로필 이미지 / 실시간 통계 / GCS 데이터 (비동기 후속)

### 제3항 [로그인 세션 유지 강화 — Session Cache Persistence]

- 로그인 성공 후 사용자 정보를 **로컬 스토리지(브라우저 캐시 또는 `st.session_state` 영속화)에 캐싱**하여 다음 구동 시 재연산 없이 즉시 출력한다.
- 캐시 저장 항목: `user_id`, `user_name`, `user_role`, `last_login`, `sidebar_prefs`.
- 캐시 유효 기간: **24시간** (만료 시 재인증 요구).
- 구현 방식: `st.session_state` 영속 키 접두사 `_s39_` 사용, 세션 간 유지가 필요한 경우 Supabase `user_session_cache` 테이블 활용.

### §구현 핵심 상수 및 함수 (app.py)

| 항목 | 역할 |
|------|------|
| `_S39_CACHE_TTL` | 세션 캐시 유효 시간 (기본값: `86400` = 24시간) |
| `_S39_CACHE_PREFIX` | 세션 영속 키 접두사 (`"_s39_"`) |
| `_s39_load_session_cache()` | 저장된 세션 캐시 로드 → 사이드바 즉시 렌더링 |
| `_s39_save_session_cache()` | 로그인 성공 후 사용자 정보 캐시 저장 |
| `_s39_sidebar_skeleton()` | 사이드바 비동기 로딩 중 shimmer 스켈레톤 반환 |

---

## 📋 Update Log

| 일자 | 건강보험료율 | 장기요양보험료율 | 비고 |
|------|------------|----------------|------|
| 2026-03-05 | 7.09% | 0.9182% | 초기 가이딩 프로토콜 등록 (2024년 기준) |

---

*최종 개정: 2026-03-05*
